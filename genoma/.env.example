# Exemplo de .env para desenvolvimento (sem segredos)
API_BASE_URL=http://localhost:3000/api
# Se true, permite aceitar certificados self-signed para localhost (apenas dev)
DEV_ALLOW_INSECURE_HTTPS=false
# Se true, ignora auth real e usa um MockAuthService para desenvolvimento
DEV_AUTH_BYPASS=false
